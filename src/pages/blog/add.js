import React from 'react'

export default class BlogAdd extends React.PureComponent {
  state = {
    str: 'Blog Add Component'
  }

  render() {
    const { str } = this.this.state.

    return (
      <div class="blog-add-container">
        {str}
      </div>
    )
  }
}
